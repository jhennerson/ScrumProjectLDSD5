import { Routes } from '@angular/router';
import { WasteBinComponent } from 'src/app/components/waste-bin/waste-bin.component';

export const WASTE_BIN_ROUTES: Routes = [
  { path: '', component: WasteBinComponent },
];
