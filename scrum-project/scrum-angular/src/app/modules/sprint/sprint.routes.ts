import { Routes } from '@angular/router';
import { SprintComponent } from 'src/app/components/sprint/sprint.component';

export const SPRINT_ROUTES: Routes = [{ path: '', component: SprintComponent }];
